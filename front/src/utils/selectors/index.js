export const statusSelector = (state) => state.shop.status;

export const productsListSelector =(state) => state.shop.products;

export const productDetailSelector = (state) => state.shop.productDetail;